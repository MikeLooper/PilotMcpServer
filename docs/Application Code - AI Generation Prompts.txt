The code in this application was generated by AI using the following prompts:
==============================================================================

Create a C# MCP server that will allow interaction with the Pilot APIs.
1. Create a standard .NET development directory structure.
2. Include the `https://github.com/MikeLooper/PilotSharedSource` repository in this workspace as a SubModule.  Ensure the latest copy of the repository is retrieved and ready for use. Update the README to describe how the SubModule can be retrieved and updated when there is a change in the SubModule.  Place the SubModule under the `shared` directory.
3. Generate the code for a standard C# MCP Server code using the following:
- Server name is: Pilot MCP Server.
- Target language: C#.
- Target framework: .NET 10.
- Project Type: Console application.
- Preferred Nuget SDK Package: ModelContextProtocol.AspNetCore
- Security & Authentication: None.
- Logging Isolation Requirements:
	A. Bypass Console.Out, since that breaks STDIO communication channels.
	B. Route cleanly via Console.Error or standard structured logging providers.
- The APIs available to consume can be found in the following table.  This information should be stored in a way that is easy to update (like a configuration), but would be included within the MCP Server to avoid modification by a bad actor:
	| Description                      | Base Host | Port  |
	| -------------------------------- | --------- | ----- |
	| .NET Core with SQL Server        | localhost | 55101 |
	| .NET Core with PostgreSQL        | localhost | 55201 |
	| Java Spring Boot with SQL Server | localhost | 55301 |
	| Java Spring Boot with PostgreSQL | localhost | 55401 |
	| Python with SQL Server           | localhost | 55701 |
	| Python with PostgreSQL           | localhost | 55801 |
- The same endpoints are available across all the APIs and are described in this OpenAPI specification: `\shared\PilotSharedSource\OpenAPI\PilotApi_v1.yaml`.
	A. Include method signatures: Names, argument types, and return values for each method to ensure strong typing.
	B. Include clear descriptions for the [Description] or [McpServerTool] custom attributes. These descriptions are critically exposed to external LLMs to help them identify when and how to invoke that specific tool.
	C. Include constraints for input parameters (e.g., defining required vs. optional arguments) to mirror JSON schema payload verification rules.
	D. Include asynchronous architecture constraints: Explicit programming patterns to enforce, ensuring the agent leverages Task and CancellationToken primitives so network and I/O processes scale concurrently.
- Provide tools for accessing the API endpoints.
- Provide a way for the user to specify which API to consume, with a default to `.NET Core with SQL Server`.
- Provide a tool that will list the available endpoints (as a summary list, not by each different API).
- Provide a tool that will list the available APIs, with the current version and deploy date from each (read from each APIs `About` endpoint).  This response would indicate which APIs are actively available or are unavailable.
4. Create unit tests to match and cover the created classes, methods, and properties. Verify all unit tests execute successfully, or repair as needed to reach successful execution.
5. Update the README to describe the current application and how it would be used.

...
